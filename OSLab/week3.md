# Week 3
```bash
cal # Show Calendar of current month
cal 2021 # Show Calendar of year 2021
> # redirect output for replace
>> # redirect output for append to old output
sh # switch to sh mode
bash # switch to bash mode
ps # show running process
jobs # show all running job
kill -9 <pid> # kill specify process
od -c <filename> # display value in base8
cat <a> <b> # concatinate file a with file b and displaycat
```
Score File
```
whoami > score
date >> score
ls -l >> score
```