# Linux Script

```sh
while [ condition ] 
do

done
# -lt => less than
# -gt => greater than
# -ne => not equal
# -le => less than equal
# -ge => greater than equal
# loop
exit <number> # exit program
arr[index]=val # Define array with val
arr[*] # Get all element for echo
$(#arr[*]) # length of array
```